services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    platform: linux/amd64
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MCP_SQL_COMMAND=/app/mcp/MssqlMcp
      - MCP_NEXUS_URL=http://backend:5000/mcp-nexus/server
    volumes:
      # Mount the MCP executable with all its dependencies
      - /home/kpisystemuser/work/nexus-mcp/bin:/app/mcp:ro
    restart: unless-stopped
    networks:
      - mcp-network

  # Nginx reverse proxy with built-in frontend
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - mcp-network

  # SQL Server for development
  # sqlserver:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.sqlserver
  #   container_name: mcp-sqlserver
  #   environment:
  #     - ACCEPT_EULA=Y
  #     - SA_PASSWORD=YourStrong!Passw0rd
  #     - MSSQL_PID=Developer
  #   ports:
  #     - "1433:1433"  
  #   volumes:
  #     - sqlserver_data:/var/opt/mssql
  #   networks:
  #     - mcp-network
  #   restart: unless-stopped

networks:
  mcp-network:
    driver: bridge

# volumes:
#   sqlserver_data:  # Not needed when using external SQL Server